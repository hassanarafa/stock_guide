import 'package:flutter/material.dart';

class SalesMainLayout extends StatelessWidget {
  const SalesMainLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Sales"));
  }
}
